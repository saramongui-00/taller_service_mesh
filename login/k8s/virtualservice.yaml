apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: login-service
spec:
  gateways:
    - my-gateway
  hosts:
    - "*"
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: login-service
            port:
              number: 80